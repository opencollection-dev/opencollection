import styled from '@emotion/styled';

/**
 * Shared page padding: 15px top/bottom (0.9375rem), 48px left/right (3rem).
 * Expressed in rems so it scales with the root font size.
 */
export const PageWrapperContainer = styled.div`
  padding: 0.9375rem 3.5rem;
`;
