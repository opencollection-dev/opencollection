import styled from '@emotion/styled';

export const RequestParamsWrapper = styled.div`
  /* The Path / Query labels: indented to line up with the table's row content
     (matches the .property-row left padding) and spaced from what's above. */
  .oc-request-params-heading {
    margin-top: 0.75rem;
    margin-left: 0.875rem;
  }
`;
