// Barrel for the empty-state icons: import them all from `assets/icons`, or
// reach for a single icon directly from its own file (e.g. `assets/icons/GlobeIcon`).
export * from './GlobeIcon';
export * from './BookIcon';
